package api

import (
	"fmt"
)
// w http.ResponseWriter, r *http.Request
func HandleVideos() {
	// Handle GET, POST, PUT, DELETE requests for videos
	// Return appropriate response codes and data
	fmt.Println("Video Handler Started...")
	//fmt.Fprintf(w, "This is the videos endpoint")
}
